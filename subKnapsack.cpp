#include <iostream>
#include <vector>
using namespace std;

int Knapsack(vector<pair<int, int> > &v, int n, int m){
	int sum = 0;
	double* per = new double[n];
	for(int i = 0; i < )
}
int main()
{
	vector<pair<int, int> > v;
	v.push_back(make_pair(60, 4));
	v.push_back(make_pair(45, 9));
	v.push_back(make_pair(21, 7));
	v.push_back(make_pair(12, 3));
}

